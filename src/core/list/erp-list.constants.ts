/** Constantes listados ERP — FRONTEND_LISTADOS_CONTRACT_V1 §7.1–7.2. */

/** Default backend cuando se envía `page` sin `limit`. */
export const ERP_LIST_DEFAULT_LIMIT = 50;

/** Límite máximo permitido por contrato. */
export const ERP_LIST_MAX_LIMIT = 100;

/** Debounce recomendado toolbar búsqueda (ms). */
export const ERP_LIST_SEARCH_DEBOUNCE_MS = 350;

/** Debounce menor para modales/combos (ms). */
export const ERP_LIST_SEARCH_DEBOUNCE_MODAL_MS = 200;
