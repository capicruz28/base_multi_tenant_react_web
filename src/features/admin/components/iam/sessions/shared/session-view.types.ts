/** Variante de presentación sesiones activas — admin vs self (MySessions). */
export type ActiveSessionsViewVariant = 'admin' | 'self';

/** Densidad shared — tabla (default) vs card admin (ritmo vertical fijo). */
export type SessionSharedLayout = 'table' | 'card';
